#include<stdio.h>
#include<stdint.h>

int main(){
	uint8_t len=10;
	uint8_t ankit[len];
//	printf("size of the aray is %d\n",sizeof(ankit));
//	printf("base address of array is %d",*ankit);
	return 0;
}
